[build-system]
requires = ["setuptools", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "franka_rt"
version = "0.1.0"
description = "Python bindings for libfranka real-time control of Franka FR3"
requires-python = ">=3.8"
dependencies = ["numpy", "pybind11"]
